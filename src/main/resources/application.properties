# Conexión a Neon (usando pgAdmin)
spring.datasource.url=jdbc:postgresql://ep-muddy-bird-amrkwie8-pooler.c-5.us-east-1.aws.neon.tech:5432/menu_digital_db?sslmode=require
spring.datasource.username=neondb_owner
spring.datasource.password=npg_IRns5uLEkmJ6
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Server
server.port=8084
server.servlet.context-path=/api
spring.application.name=service-menudigtal

# Exponer el endpoint de health
management.endpoints.web.exposure.include=health,info,metrics

# Mostrar detalles del health check (opcional, pero útil)
management.endpoint.health.show-details=always